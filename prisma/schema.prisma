generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  name         String
  /// admin | editor | gerencia | viewer
  role         String   @default("editor")
  active       Boolean  @default(true)
  /// Nombre que se muestra en Proyecto (p. ej. Atcotrans, TMS 2.0)
  projectName  String   @default("")
  company      String   @default("")
  projectArea  String   @default("")
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

/// Estado compartido del tablero (equivalente al localStorage actual)
model Workspace {
  id        String   @id @default("default")
  /// JSON: { doc, dev, stageEdits, reqOrder, customStages, decisionGlobal }
  payload   String
  updatedAt DateTime @updatedAt
  updatedBy String?
}
